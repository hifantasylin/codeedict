# 项目架构地图 — <projectId>

> 自动扫描生成于 <日期>。记录项目特有的架构惯例，供分析师写方案、审查官对账时参考。

## 可复用组件

> 项目中已有的公共组件/工具类。新增功能时应优先复用。

| 组件 | 用途 | 位置 | 强制 |
|------|------|------|:---:|
| <组件名> | <什么场景用> | <路径> | ✅/— |

## 命名规则

> 不同模块/类型的命名惯例。

| 类型 | 规则 | 示例 |
|------|------|------|
| <如：弹窗组件> | XxxOverlayView + XxxManager 成对 | LoginOverlayView |

## 架构分层

> 各层职责与越界规则。

| 层 | 职责 | 禁止 |
|----|------|------|
| UI/View | 界面渲染 + 交互 | 不直接调 DB/网络 |
| ViewModel | 状态管理 + 业务逻辑 | 不持有 Context/View 引用 |
| Repository | 数据源封装 | 不持有 UI 状态 |

## 反模式

> 项目中明确禁止的写法。审查官看到直接标记偏离。

| 反模式 | 正确做法 |
|--------|---------|
| <如：Activity 里写业务逻辑> | <抽到 ViewModel> |

## 设计约束

> 人为注入的硬性规则。反模式是扫描代码发现的"实然"，这里是规定的"应然"。
> 由项目维护者手动编辑，审查官逐条对照。提议者不得驳回。

| # | 约束 | 说明 |
|---|------|------|
| <1> | <如：配置走 config.yaml 不走 .env> | <.env 只放密钥> |
| <2> | <如：无消费者不建抽象> | <新增接口必须有至少一个调用者> |

## 变更记录

| 日期 | 来源 | 变更内容 |
|------|------|----------|
| <日期> | 🏗️ 初始化 | 自动扫描生成 |
