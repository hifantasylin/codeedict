# 项目架构地图 — <projectId>

> 自动扫描生成于 <日期>。记录项目特有的架构惯例，供分析师写方案、审查官对账时参考。

## 技术栈

> AI 诊断建议，含选型理由。可执行命令见 `project.json → toolchain`。

| 项 | 选型 | 理由 |
|----|------|------|
| 语言 | <语言> | <理由> |
| 框架 | <框架及版本> | <理由> |
| 构建工具 | <构建工具> | <理由> |
| 包管理器 | <npm/yarn/pnpm> | <理由> |
| 运行时 / 部署 | <Node 版本 / Docker / 云平台> | <理由> |

## 项目结构

> 顶层目录组织和文件组织方式。

| 项 | 约定 | 理由 |
|----|------|------|
| 组织方式 | <按功能 feature-based / 按类型 type-based> | <理由> |
| 顶层目录 | <src/pages, src/components, ...> | <惯例> |
| 入口文件 | <应用入口路径> | <说明> |

## 可复用组件

> 项目中已有的公共组件/工具类。新增功能时应优先复用。

| 组件 | 用途 | 位置 | 强制 |
|------|------|------|:---:|
| <组件名> | <什么场景用> | <路径> | ✅/— |

## 命名规则

> 不同模块/类型的命名惯例。

| 类型 | 规则 | 示例 |
|------|------|------|
| 文件 | <kebab-case / camelCase / PascalCase> | <示例> |
| 变量 / 函数 | <惯例> | <示例> |
| 组件 / 类 | <PascalCase + 职责后缀> | <示例> |
| 目录 | <小写 / 复数 / 功能前缀> | <示例> |

## 架构分层

> 各层职责与越界规则。

| 层 | 职责 | 禁止 |
|----|------|------|
| UI/View | 界面渲染 + 交互 | 不直接调 DB/网络 |
| ViewModel | 状态管理 + 业务逻辑 | 不持有 Context/View 引用 |
| Repository | 数据源封装 | 不持有 UI 状态 |

## 边界标记

> 🔒 核心逻辑（修改影响大，需变更控制） / ✅ 可自由修改 / ⚠️ 需人工 review

| 标记 | 模块/文件 | 原因 |
|------|----------|------|
| 🔒 | <如：src/auth/> | <认证逻辑，影响安全> |
| 🔒 | <如：src/model/> | <数据模型，被多处引用> |
| ⚠️ | <如：src/api/> | <接口层，变更涉及调用方适配> |
| ✅ | <如：src/components/ui/> | <纯 UI 组件，变更影响小> |

## 依赖关系图

> Init 自动扫描生成。Archive 阶段增量更新。规范刷新时全量重建。

| 文件 | 依赖 | 被依赖 |
|------|------|--------|
| <文件路径> | <它 import 了谁> | <谁 import 了它> |

## 反模式

> 项目中明确禁止的写法。审查官看到直接标记偏离。Archive 阶段自动从 bugfix 提取追加。

| 症状关键词 | 错误模式 | 正确做法 | 来源任务 |
|-----------|---------|---------|---------|
| <如：loading 卡住, 进度不更新> | <如：修改状态枚举不同步调用方> | <如：改枚举前搜索全项目引用> | <TASK-XXX> |

## 设计约束

> 人为注入的硬性规则。由项目维护者手动编辑，审查官逐条对照。提议者不得驳回。

| # | 约束 | 说明 |
|---|------|------|
| <1> | <如：配置走 config.yaml 不走 .env> | <.env 只放密钥> |
| <2> | <如：无消费者不建抽象> | <新增接口必须有至少一个调用者> |

## 变更记录

| 日期 | 来源 | 变更内容 |
|------|------|----------|
| <日期> | 🏗️ 初始化 | AI 诊断生成 |
